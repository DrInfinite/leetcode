# Leetcode Solutions

A repository containing my leetcode solutions when attempting daily problems
using C++ (because I like the challenge)

This repository and all the code files are licensed under [The Unlicense License](./LICENSE)

To execute the latest solved problem, run the following command:

```bash
g++ ./src/main.cc -o ./build/main.out -O2 && ./build/main.out
```
